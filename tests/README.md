# Automated testing strategy

The suite uses standard-library `unittest` tests. It covers capture/source contracts and classic PCAP input; Ethernet, IPv4/IPv6 and transport analysis; flow identity, lifecycle, statistics and features; detector configurations, predicates and findings; evaluation, truth, metrics and reporting; datasets, experiments, version references, end-to-end composition, performance methodology, diagnostics, and the CLI.

The verified interpreter is Python 3.9.6. Run from the repository root:

```sh
PYTHONPATH=src python3 -B -m unittest discover -s tests
```

Verify every declared package export:

```sh
PYTHONPATH=src python3 -B -c 'import analysis, application, capture, detection, ml; packages = (analysis, application, capture, detection, ml); assert all(hasattr(package, name) for package in packages for name in package.__all__); print("all public exports resolve")'
```

No installation, third-party dependencies, network services, live-capture privileges, or running VMs are required. Fixtures use synthetic bytes and explicit observations; PCAP and CLI tests create temporary local files. Performance tests use controlled clocks for exact timing assertions and do not set machine-speed thresholds. The suite checks normal results, ordering, immutable inputs/results, malformed inputs, failure propagation, and execution counts.

| Existing verification area | Representative tests |
| --- | --- |
| Capture and lifecycle | [PCAP source](test_pcap_packet_source.py), [capture execution](test_capture_execution.py). |
| Protocol and fragment boundaries | [packet outcomes](test_packet_analysis_outcome.py), [IPv6 transport](test_ipv6_transport.py), [fragmentation](test_ipv6_fragmentation.py), [ICMPv6](test_icmpv6.py). |
| Directional TCP stream observation | [Sequence contract](test_tcp_stream_observation.py) and [flow/LDAP integration](test_tcp_stream_integration.py): exact bytes/ranges, wraparound, gaps, overlaps, controls, resource limits, directional isolation, publication failures, PCAP parity, and structural determinism. |
| Incremental LDAP framing | [Framing](test_ldap_stream_framing.py), [TCP consumption](test_tcp_stream_consumption.py), and [lifecycle](test_ldap_stream_lifecycle.py): exact batches/boundaries/suffixes, malformed stops, bounded unsupported messages, storage reclamation, parser call counts, directional IPv4/IPv6 isolation, immutable publication failures, PCAP parity, and structural determinism. |
| LDAP request/response correlation | [Association policy](test_ldap_correlation.py) and [lifecycle](test_ldap_correlation_lifecycle.py): exact references/directions, non-FIFO replies, Search continuations, ambiguous reuse, unavailable streams, bounded pending state, final unresolved projections, publication failures, IPv4/IPv6 PCAP parity, unchanged detection/research results, and structural determinism. |
| LDAP request termination summaries | [Summary updates](test_ldap_request_summary.py) and [lifecycle](test_ldap_request_summary_lifecycle.py): exact request/terminal references, Search response counts, ambiguity/unavailability, bounded storage, allocation/publication failures, finalization, IPv4/IPv6 PCAP parity, detector/research compatibility, and structural determinism. |
| LDAP protocol foundation | [Envelope parsing](test_ldap.py) and [flow integration](test_ldap_flow_statistics.py): operation tags, truncation, BER bounds, controls, resource limits, coalesced and split observations, IPv4/IPv6, immutable publication, PCAP/detection parity, and hash-seed/timezone determinism. |
| Flow and feature semantics | [observation windows](test_flow_observation_window.py), [snapshots](test_flow_feature_snapshot.py), [IPv6 features](test_ipv6_features.py). |
| Active-flow capacity | [Manager contracts](test_flow_capacity.py) and [pipeline integration](test_flow_capacity_pipeline.py): exact bounds, timestamp/sequence victim order, mixed IPv4/IPv6 TCP/UDP churn, state release, immutable publication and table-allocation failures, retry, LDAP finalization, capture/consumer failures, PCAP evaluation configuration, unchanged below-capacity results, five-field findings, and hash-seed/timezone determinism. [CLI tests](test_cli.py) cover optional limit validation and capacity JSON. |
| Detection and application composition | [orchestration](test_detector_orchestration.py), [pipeline](test_detection_pipeline.py), [end-to-end validation](test_end_to_end_validation.py). |
| Incremental finding delivery | [Delivery contracts](test_detection_stream.py) and [failure contracts](test_detection_stream_failures.py): synchronous observation/closure/detector order, exact collecting-path equivalence, lazy-input release of historical objects, caller retention, IPv4/IPv6 TCP/UDP and PCAP parity, LDAP/export isolation, failed detector batches, consumer/capture/publication/finalization errors, retry boundaries, and byte-identical replay across hash seeds/time zones. |
| Evaluation and presentation-neutral results | [evaluation](test_detection_evaluation.py), [metrics](test_detection_metrics.py), [reporting](test_evaluation_report.py). |
| Incremental evaluation | [Matching and state contracts](test_incremental_detection_evaluation.py) and [stream integration](test_incremental_evaluation_stream.py): exhaustive short decision sequences, historical preference/multiplicity equivalence, packet positions, deferred flow order, equality fallback, linear ordinary lookup, explicit retention/release, terminal failures and abort, external truth, IPv4/IPv6 TCP/UDP, all PCAP encodings, unchanged metrics, and hash-seed/time-zone determinism. |
| Incremental metrics | [Accumulator contracts](test_incremental_detection_metrics.py) and [stream integration](test_incremental_metrics_stream.py): exact classification counts and ratios, exhaustive sequence equivalence, arbitrary-size integer/underflow behavior, terminal lifecycle and partial-input failures, fixed counter state and reference release, source isolation, IPv4/IPv6 TCP/UDP, LDAP, all classic PCAP encodings, and hash-seed/time-zone replay. |
| Streaming end-to-end evaluation | [Execution contracts](test_streaming_evaluation.py) and [failure contracts](test_streaming_evaluation_failures.py): real collecting-path equivalence, exact delegation and finalization order, separate channels, external truth, lazy-source reference release, fail-fast validation, terminal abort behavior and exception precedence, IPv4/IPv6 TCP/UDP, LDAP, all PCAP encodings, and deterministic replay. Flow-only finding production is not a configured mode; flow-only truth is tested with mandatory packet detection preserved. |
| Dataset and execution methodology | [datasets](test_detection_dataset.py), [experiments](test_detection_experiment.py), [case benchmarks](test_detection_benchmark.py), [performance benchmarks](test_performance_benchmark.py). |
| Operational boundaries | [diagnostics](test_operational_diagnostics.py), [CLI](test_cli.py). |
| ML input boundary | [Feature projection](test_ml_feature_projection.py): canonical values and ordering, direct scalar preservation, availability, version rejection, immutable factory-only construction, label/detector exclusion, future-state isolation, pipeline independence, and hash-seed/timezone equivalence. |
| Research example boundary | [Research examples](test_research_example.py), [observation association](test_research_observation.py), [timestamp ownership](test_research_observation_timestamps.py), and [direct construction](test_research_construction.py): projection/truth retention, same-window derivation, optional closed-window context, nested timestamp validation, immutable ownership, validation, equality/repeats, outcome-based PCAP regression, and complete-output equivalence across hash seeds/timezones with detector/application imports blocked. |
| Research dataset boundary | [Research datasets](test_research_dataset.py): exact ordered membership, preserved repeats, empty datasets, value equality, caller-list isolation, invalid collections/members, no feature/truth inspection, and detector-independent construction across hash seeds/timezones. |

Tests verify implemented predicates and composition, not production attack-detection coverage. There are no CPU/memory profiling or throughput acceptance guarantees. Future tests for storage, enrichment, alerting, deployment, or resource exhaustion require those capabilities to be implemented first. Do not manufacture tests to meet a count or weaken failures to match documentation.

Small synthetic or sanitized fixtures require documented provenance and redistribution permission. Raw captures are ignored by default; review any fixture exception before committing it. Keep credentials, large datasets, and generated reports out of version control. The ignored root `artifacts/` location is a development convenience, not an implemented storage service.

## Curated PCAP scenarios

[Scenario helpers](pcap_scenarios.py) construct original synthetic Ethernet/IP bytes using the existing packet and classic-PCAP construction helpers. These fixtures contain no third-party traffic. TCP sequence/acknowledgment numbers describe a handshake, three-byte transfer and server half-close; UDP carries opaque request/response bytes with a repeated request. Fixtures include IPv4 header checksums and TCP/UDP pseudo-header checksums for both address families. Short Ethernet frames include padding without an FCS. Payloads do not imply application-protocol analysis. IPv6 transport checksums are fixture data; the current IPv6 analyzer does not validate them.

[Scenario tests](test_pcap_scenarios.py) write named, temporary PCAPs with short protocol scenarios and longer periodic-flow regressions and execute them through `PcapPacketSource`. They cover bidirectional IPv4/IPv6 TCP/UDP exchanges, interleaved families, SYN retransmission windows below/equal/above detector thresholds, Hop-by-Hop/Destination Options with atomic fragments, mixed malformed headers, and complementary UDP fragments. All timestamps are explicit; representative mixed traffic uses little-endian microsecond and big-endian nanosecond PCAP encodings. Repeated reads and CLI subprocess runs verify equivalent outputs. Temporary files are cleaned up; no binary captures are committed.

Complementary IPv6 fragments deliberately do not become a reassembled flow: an incomplete first UDP fragment remains an analysis failure, and the non-first fragment reaches the existing flow-admission error, preventing evaluation. ICMPv6's analysis/admission distinction, empty PCAPs, malformed PCAP records and all four encoding variants remain covered by the existing [system regressions](test_system_regressions.py), [end-to-end tests](test_end_to_end_validation.py) and [source tests](test_pcap_packet_source.py).

### Protocol coverage review

The matrix records coverage inspected at `7f27d98` and the selected additions in the scenario tests. Unit coverage includes tests composed from in-memory observations; the PCAP column describes execution through temporary wire-format captures. The additions preserve current contracts and do not broaden protocol support.

| Protocol / packet form | Existing unit coverage | Existing PCAP/system coverage at review | Selected regression and architectural purpose |
| --- | --- | --- | --- |
| IPv4/IPv6 TCP and UDP exchanges | [Flow identity](test_flow_identity.py), [IPv6 flows](test_ipv6_flow.py), directional statistics and features | Bidirectional exchanges, payload, ACK, half-close, retransmissions, thresholds, mixed families | Interleave TCP and UDP with identical addresses and ports in each family. Separate windows, byte/direction totals and detector counts prevent transport-state contamination. TCP options retain payload boundaries; RST increments its counter without closing the window. |
| TCP/UDP checksum fields | [Outcomes](test_packet_analysis_outcome.py), [TCP checksum](test_tcp_checksum.py), [UDP checksum](test_udp_checksum.py), [IPv6 transport](test_ipv6_transport.py) | Valid exchanges and IPv4 header checksum failure | Corrupt transport checksums exclude IPv4 packets from flows; omitted IPv4 UDP checksums remain admissible. The same IPv6 checksum fields remain observed without validation. Exact packet metrics and admitted counts protect this distinction. |
| UDP lengths and Ethernet trailing bytes | [UDP](test_udp.py), [IPv6 transport](test_ipv6_transport.py) | UDP payload exchanges; malformed TCP/extension sequences | Both families distinguish length below eight, length beyond IP payload and short headers. Ethernet padding cannot repair them. Excess IP bytes stay outside UDP payload but captured bytes remain included in flow volume. |
| IPv4 ICMP | [ICMP](test_icmp.py), [ICMP checksum](test_icmp_checksum.py), [Outcomes](test_packet_analysis_outcome.py) | Generic unsupported IPv4 transport admission; no dedicated ICMP capture scenario | A checksummed echo message succeeds at capture analysis but stops the full pipeline at flow admission. One packet analysis/detection, no feature extraction or evaluation, and no delivery of the following packet. |
| Failed ICMPv4/ICMPv6 messages | [Outcomes](test_packet_analysis_outcome.py), [ICMPv6](test_icmpv6.py) | Basic successful ICMPv6 analysis followed by admission failure | Corrupt ICMPv4 and short ICMPv4/ICMPv6 headers between valid UDP packets preserve integrity/incomplete classifications and create no flow state. Short ICMPv6 follows Destination Options and an atomic Fragment Header. Positive expectations retain historical NOT_EVALUABLE false negatives. |
| Non-initial IPv4 TCP/UDP/ICMP fragments | [Outcomes](test_packet_analysis_outcome.py) | IPv6 non-first/complementary fragment rejection | Final and intermediate IPv4 fragments remain unsupported, non-evaluable packet findings while subsequent valid datagrams continue the existing flow. No fragment payload is interpreted as a transport header. |
| Initial IPv6 TCP fragment and variable extension lengths | [IPv6 transport](test_ipv6_transport.py), [IPv6 detection](test_ipv6_detection.py) | Fixed-length Hop-by-Hop/Routing/Destination Options, atomic fragments, incomplete first UDP fragment | A complete TCP header with options in an initial fragment supplies only available payload. A following packet uses 24-byte Routing and 16-byte Destination Options headers; an atomic fragment shares the same flow. Exact payload, offsets, bytes and counters assert packet-local admission without reassembly. |
| Opaque IPv6 terminal selectors | [ICMPv6](test_icmpv6.py), [IPv6 detection](test_ipv6_detection.py), [in-memory pipeline](test_detection_pipeline.py) | No dedicated opaque-selector capture sequence | ESP, AH, No Next Header and an unknown selector after Destination Options must not scan TCP-looking payload. Admission fails after two observations, finalizes only the preceding UDP window, and never evaluates or consumes the following TCP packet. |
| Empty/truncated PCAP, encoding variants, malformed TCP/extensions | Source, outcome and protocol unit tests | Already covered by source, system, end-to-end and scenario tests | Retained without duplicating existing scenarios. |

These eight additional test methods use subtests for related protocol cases; subtests are not counted as separate tests. Assertions use production outcomes, findings, flow snapshots and reports. Failure scenarios also check source cleanup and relevant execution counts. Existing packet/PCAP helpers construct the wire data; the shared transport helper additionally supports TCP option bytes.

This review is not an exhaustive protocol matrix. IPv4 option combinations, repeated extension headers, and ICMPv6 partial-fragment combinations retain unit coverage without new dedicated PCAP scenarios here. Reassembly, IPv6 checksum validation, AH/ESP decoding, ICMP subtype semantics, VLAN decoding and application semantics beyond LDAP envelopes remain unimplemented; tests do not invent them.

## File-acquisition reliability regressions

The security/reliability review at `5515814` identified an acquisition defect: opening a FIFO before checking its file type could wait for a writer instead of reaching the existing regular-file rejection. The [source correction](../src/capture/README.md#classic-pcap-packet-source) checks before opening, retains descriptor validation, and requests nonblocking acquisition where supported.

Six additional tests establish the following specific properties:

- [PCAP source tests](test_pcap_packet_source.py) reject FIFO, FIFO-symlink and directory paths before opening; preserve terminal source state and zero delivery; replace a regular path with a FIFO at the open boundary without concurrency and verify rejection before reads plus descriptor closure; and preserve metadata-error causes and ownership-sensitive cleanup.
- [System regressions](test_system_regressions.py) repeat rejected startup and assert zero analysis, feature extraction, detector, evaluation, metric and report calls. Subsequent valid captures produce equivalent reports. Symlinks to regular captures retain equivalent packet/flow results across repeated runs.
- [CLI tests](test_cli.py) repeatedly execute FIFO and FIFO-symlink inputs in subprocesses and require exactly exit 1, empty stdout and the existing capture-error JSON on stderr. A subprocess timeout is a hang safeguard, not an elapsed-time or throughput assertion.

Temporary FIFOs, symlinks and captures are removed by the existing temporary-directory cleanup. FIFO tests are conditional on platform support; the replacement test additionally requires `os.O_NONBLOCK`. Existing handle-tracking tests intercept `io.open`, the acquisition boundary now used to supply an opener, with their read, close, ordering and error-cause assertions retained.

The review also checked existing parser bounds, maximum repeated IPv6 extension traversal, checksum/outcome distinctions, flow admission and state isolation, lifecycle/error precedence, feature validation, finding construction, evaluation, diagnostics and deterministic execution. Their established regressions remain in place, including historical NOT_EVALUABLE positive-expectation false negatives. This pass adds no protocol or detector semantics. Per-record memory can scale with the represented capture length; accumulated findings and active flows scale with input volume, and new-flow insertion copies the active mapping. No global memory/CPU limit, arbitrary-filesystem timeout or immunity to resource exhaustion is established. The input-stability requirement and existing protocol limitations remain unchanged.

## Accumulated inter-arrival roundoff regressions

The final readiness review reproduced valid periodic flows failing feature extraction because the negative-variance allowance omitted rounding accumulated across intervals. Three focused tests cover global and independent directional float sums, continued rejection of materially inconsistent moments, and 1,002-packet bidirectional TCP/UDP captures in both IP families. Complete PCAP-to-report runs repeat with fresh sources and compare full results, exact truth metrics, interval features, feature-contract provenance and cleanup. The population formulas, raw accumulators, public schemas and historical NOT_EVALUABLE semantics remain unchanged.

Authorized lab methodology belongs to [labs](../labs/README.md). Portable checks derived from future lab results should become regressions where appropriate. The test count records the verified baseline and is not a coverage target.

## DNS protocol foundation

`test_dns.py` checks header and section contracts, binary names, compression boundaries and loops, message/entry/traversal limits, opaque RDATA, malformed prefixes, immutable APIs and bounded reproducible adversarial input. `test_dns_packet_analysis.py` checks real IPv4/IPv6 UDP observations, extension headers, external TCP message delimitation, all four classic PCAP encodings, unchanged flow/detection/evaluation behavior, capture failure propagation and identical serialized observations across three hash seeds and three time zones. Run both with `PYTHONPATH=src python3 -B -m unittest tests.test_dns tests.test_dns_packet_analysis`.

## DNS transaction correlation

`test_dns_correlation.py` checks immutable request/response associations, direction/question/opcode matching, ID reuse, ambiguity, pending-state saturation, invalid DNS exclusion, timestamp contracts and bounded state without history. `test_dns_correlation_lifecycle.py` uses real UDP packet analysis and flow/session admission for IPv4/IPv6 isolation, publication failure/retry, all closure reasons, capture/consumer failures, client-port isolation, PCAP equivalence and deterministic state churn. Replay runs all nine combinations of hash seeds 1/29/503 and UTC/Asia-Kolkata/America-New-York time zones. Run with `PYTHONPATH=src python3 -B -m unittest tests.test_dns_correlation tests.test_dns_correlation_lifecycle`.

## DNS transaction-derived statistics

`test_dns_transaction_statistics.py` verifies exact status/message accounting, every opcode and header response-code bin, record limits, empty values, immutable public state, input validation, integer totals beyond float precision/range, rational latency means, delimited TCP observations and source isolation. `test_dns_transaction_statistics_lifecycle.py` exercises real packet analysis through flow/session correlation and aggregation, IPv4/IPv6 interleaving, repeated IDs, every closure path, failed-publication retry, pending saturation, malformed input exclusion and classic PCAP in both byte orders and both timestamp resolutions. Weak references prove completed packets/messages/transactions and released aggregates are reclaimable. Deterministic churn checks terminal accounting across active and evicted owners, and subprocess replay compares all nine hash-seed/timezone combinations.

Run the focused suite with `PYTHONPATH=src python3 -B -m unittest tests.test_dns_transaction_statistics tests.test_dns_transaction_statistics_lifecycle`. Existing parser/correlation, generic feature-contract, LDAP, detector, evaluation and metrics regressions remain required. Coordinator field-list and legacy-constructor checks include the additive protocol-specific result; detector evidence comparisons strip both DNS correlation and statistics when testing a disabled DNS observation path, preserving the original semantic assertions.

## DNS query-name structural statistics

`test_dns_query_name_statistics.py` verifies question-only accounting, root and empty semantics, expanded wire and label-payload lengths, exact rational means, byte-class boundaries, binary/case preservation, compression-equivalent measurements, parser maxima, immutable scalar state, large integer precision and parser delegation/source isolation. `test_dns_query_name_statistics_lifecycle.py` exercises actual packet/session/correlation integration, IPv4/IPv6 and client-port isolation, all terminal statuses and closure reasons, readmission, partial-aggregation/publication failure retry, malformed-prefix exclusion and all four classic PCAP encodings. Weak references check source-graph release both during active correlation and after flow release. Deterministic repeated/distinct names and flow churn verify fixed scalar shape and equality with Feature 14's question accounting. Replay covers all nine combinations of hash seeds 1/29/503 and UTC/Asia-Kolkata/America-New-York time zones.

Run with `PYTHONPATH=src python3 -B -m unittest tests.test_dns_query_name_statistics tests.test_dns_query_name_statistics_lifecycle`. Existing regression fixtures accommodate the added scalar field while preserving their original identity, constructor, detector and evaluation assertions. The DNS packet fixture accepts an optional client port for real cross-flow tests. Generic feature-contract and 49-value ML projection compatibility remain explicitly checked.

## DNS resource-record structural statistics

`test_dns_resource_record_statistics.py` checks section-only accounting, opaque RDATA byte lengths, zero/empty distinctions, parser-supported maximum length, exact `Fraction` means, unknown 16-bit type/class codes, fixed immutable distributions, duplicate multiplicity, all terminal statuses and already-delimited TCP transactions. Invalid types/statuses/messages and PENDING observations are rejected. Spies prohibit reading RDATA or owner names and reparsing payloads; structural inspection checks the reducer's source dependencies. Large synthetic counters preserve integer precision.

`test_dns_resource_record_statistics_lifecycle.py` exercises actual packet analysis through flow/session correlation and aggregation: IPv4/IPv6 and client-port isolation, UDP, unframed TCP payload exclusion, closure/readmission/eviction, completed-prefix accounting, partial aggregation and failed-publication retry, infrastructure failures and malformed-prefix exclusion. Weak references prove retained statistics release packets, messages, records, names, transactions and flow/window state. Bounded-shape tests cover repeated records, every distribution block and flow churn. All four classic PCAP encodings produce equivalent statistics; subprocess replay checks all nine combinations of hash seeds 1/29/503 and UTC/Asia-Kolkata/America-New-York time zones. Explicit regressions preserve generic version 1 and the 49-value ML projection. These observations do not detect attacks.

Run with `PYTHONPATH=src python3 -B -m unittest tests.test_dns_resource_record_statistics tests.test_dns_resource_record_statistics_lifecycle`. Existing constructor/field-list fixtures accommodate only the additive coordinator field. DNS parser/correlation, previous DNS statistics, LDAP, detector, evaluator and metrics regressions remain required; their implementations are unchanged.

## DNS message flag statistics

`test_dns_message_flag_statistics.py` verifies all seven semantic control properties, query/response/total accounting, all 128 supported combinations and every pair, matched and other terminal observations, exclusion of reserved information, immutable scalar state, exact large integers, constructor invariants and invalid-input rejection. Header-property spies fail on raw flag-word access and verify each semantic property is consumed once. AST inspection prohibits flag-word decoding, raw payload access, section traversal and duplicate parsing. Tests cover already-delimited TCP transactions, repeated observations and source release.

`test_dns_message_flag_statistics_lifecycle.py` exercises real packet/session/correlation integration for IPv4/IPv6 and UDP, interleaved client ports, transaction-ID reuse, all closure paths, bounded churn conserving all eight counters, completed-prefix handling and readmission. Failure tests cover nonempty prior state, partial message/transaction aggregation, publication retry, parser infrastructure and capture failure. Weak references verify source-graph release during active flows and after closure. Explicit checks preserve Features 14–18, version 1 and every value in the 49-value ML projection. All four classic PCAP encodings and all nine hash-seed/timezone combinations produce equivalent flag statistics.

Run with `PYTHONPATH=src python3 -B -m unittest tests.test_dns_message_flag_statistics tests.test_dns_message_flag_statistics_lifecycle`. AA/RD/RA/AD/CD now contribute to the existing statistics contract while original QR/TC measurements and three-argument construction remain compatible. Tests cover late-property failures in matched batches, retry without double counting and reserved-bit exclusion. This feature does not detect attacks.

## Semantic DNS header control flags

`test_dns_header_control_flags.py` checks each QR/AA/TC/RD/RA/AD/CD bit on the wire, all 128 supported combinations, independent bit changes and all 65,536 raw flag words. Tests preserve QR/TC behavior, opcode/response-code extraction, all historical Z positions, exact booleans, frozen factory-only construction, stored-field serialization and source release. The existing 2,153-input adversarial parser corpus is compared against a digest of its original stored fields, excluding only the additive absent EDNS field, proving status/section/metadata compatibility.

Real IPv4/IPv6 UDP packets, interleaved flows and IDs, already-delimited TCP transactions, parser delegation, invalid-message exclusion, infrastructure failures and all four classic PCAP encodings exercise the established architecture. Feature 17 compatibility assertions preserve the original message/response/truncated values for every supported flag combination, alongside the additive counters. AST inspection checks that downstream DNS statistics and lifecycle modules never access the raw flag word. Replay verifies all nine hash-seed/timezone combinations.

Run with `PYTHONPATH=src python3 -B -m unittest tests.test_dns_header_control_flags tests.test_dns tests.test_dns_message_flag_statistics tests.test_dns_message_flag_statistics_lifecycle`. Existing DNS and NIDS regressions remain required. No detection feature is introduced.


## EDNS(0) protocol analysis

`test_dns_edns.py` uses actual DNS wire messages to verify OPT fields, all extended-code/version values, DO/AD independence, reserved flags, unknown and repeated options, root/compressed-root handling, exact raw-record compatibility and immutable factory-only values. Boundary tests cover truncated record envelopes/RDATA, malformed option envelopes, RDLENGTH isolation, invalid owners/sections, duplicate OPT, invalid later records and deferred EDNS publication. Maximum count, total bytes and individual payload cases reach the documented bounds; excessive count and bytes stop deterministically. Seeded adversarial inputs, source-release checks, name-decoder delegation and allocation failures exercise the existing parser.

`test_dns_edns_integration.py` exercises real IPv4/IPv6 UDP packets, extension headers, already-delimited TCP transactions, interleaved client ports/IDs, sequential reuse and flow readmission. Tests compare all existing DNS statistics and all 49 projected generic feature values with semantic EDNS attachments removed from otherwise identical parsed messages. OPT remains one additional record; extended RCODE and DO remain separate from header RCODE and AD. Malformed/incomplete/unsupported EDNS cannot enter correlation or statistics. Parser/capture failures and packet-analysis failures preserve existing behavior, and retained EDNS releases the packet/message/transaction/flow graph.

All four classic PCAP encodings and all nine hash-seed/timezone combinations must yield equivalent EDNS output. Run focused coverage with `PYTHONPATH=src python3 -B -m unittest tests.test_dns_edns tests.test_dns_edns_integration`, then existing DNS and NIDS regressions and the complete suite. Historical OPT fixtures that used invalid option envelopes now use valid envelopes to test unchanged raw-record statistics. Feature 20 supplies protocol observations to the Feature 21 statistics below; neither detects attacks.

## DNS EDNS option structural statistics

`test_dns_edns_statistics.py` checks the twelve scalar fields and three fixed distribution domains, exact rational arithmetic beyond floating-point range, empty/absent OPT, zero-length and repeated options, version/extended-RCODE domains, option-code block boundaries, opaque unknown-option semantics, DO/AD independence, reserved flags, terminal accounting, constructor invariants, parser bounds, immutable snapshots and semantic-only source access. A constructor regression ensures the observed maximum leaves at least one byte for every other positive-length option.

`test_dns_edns_statistics_lifecycle.py` covers pending/matched/ambiguous/unmatched/unresolved accounting, repeated finalization, terminal prefixes, pending capacity, window churn, inactivity/readmission, IPv4/IPv6/extensions and client-port isolation, invalid-message exclusion, direct delimited TCP observations, and failed aggregation/publication/closure retries. Weak-reference tests check complete source-graph release and aggregate release. Comparisons preserve earlier DNS statistics, contract version 1 and all 49 projected values.

The observable replay includes every stored scalar, every distribution bin and all derived properties across flow churn and closure. Its SHA-256 output must agree across all nine combinations of `PYTHONHASHSEED=1,29,503` and `TZ=UTC,Asia/Kolkata,America/New_York`. PCAP validation reuses the pre-existing Feature 20 `packets()` traffic and `pcap_bytes` encoding helper through the real capture/session path in little/big-endian microsecond/nanosecond forms. This exercises existing synthetic traffic and encoding capability; it is not validation against an external captured corpus or submicrosecond timestamp preservation.

Run focused coverage with `PYTHONPATH=src python3 -B -m unittest tests.test_dns_edns_statistics tests.test_dns_edns_statistics_lifecycle`, followed by the existing DNS/protocol/NIDS regressions and complete suite. Compatibility edits to older tests only exclude the newly appended aggregate from legacy positional-constructor or identity-bearing-component fixtures, or extend exact coordinated-state field expectations. Production changes are limited to the EDNS reducer, exports and required coordinator/window integration.

## DNS-over-TCP message framing

`test_dns_stream_framing.py` exercises empty states, exact two-byte network-order prefixes, every split position, one-byte and seeded arbitrary segmentation, multiple frames, partial suffixes, zero-length frames, maximum-size messages across real TCP buffer reclamation, directional isolation, retransmission suppression, sequence wraparound, FIN/RST, gaps, conflicts, overlap and IPv6 fragmentation. Frozen factory-only values, consumption ownership, source release and a long stream verify bounded state without completed-frame history. AST checks restrict DNS dependencies to the existing message-size constant.

The actual parser limit is also the maximum two-byte unsigned value. The test for `DNS_MAX_MESSAGE_BYTES + 1` verifies that 65,536 cannot be encoded as a length prefix. A separate fault-injection test lowers the imported limit to exercise the defensive LIMIT_EXCEEDED guard without retaining its body. This does not claim naturally oversized wire declarations are possible.

`test_dns_stream_lifecycle.py` uses existing packet construction, checksum, stream, flow/session and PCAP infrastructure. It verifies segmented/coalesced requests and responses, completion timestamps, repeated/ambiguous IDs, pending capacity, IPv4/IPv6/client-port interleaving, simultaneous opposite-direction partial frames, no-OPT/EDNS cases, all Features 14–21 against the already-delimited path, parser statuses, incomplete closure and repeated finalization. Fault injection checks parser/stream/aggregation/publication/closure failures and retry; capture failures preserve cleanup and prior output. Weak references verify completed DNS/transaction graph release and framing-owner release. Generic version 1 and all 49 projection values are compared with DNS ingestion disabled; the LDAP port-collision case preserves its existing consumer.

The real segmented TCP/EDNS traffic runs through little-/big-endian microsecond/nanosecond PCAP encodings with equivalent observable output. This uses synthetic protocol fixtures and the existing `pcap_bytes` helper, not an external captured corpus. The replay compares framing metadata, closure ordering and all DNS aggregate fields under every combination of `PYTHONHASHSEED=1,29,503` and `TZ=UTC,Asia/Kolkata,America/New_York`. It does not claim submicrosecond timestamp preservation or gap recovery.

Run `PYTHONPATH=src python3 -B -m unittest tests.test_dns_stream_framing tests.test_dns_stream_lifecycle`, followed by existing TCP streams, DNS/Features 14–21, IPv4/IPv6/UDP/TCP, NIDS regressions and the complete suite. Older positional-constructor fixtures exclude the appended `dns_stream_state`, and coordinated-state field expectations include it. Older unframed-TCP fixtures still cannot bypass length framing; automatic framed TCP behavior is covered here.


## TLS record framing

`test_tls_record_framing.py` checks the exact five-byte header, all header/payload split positions, one-byte and seeded fragmentation, coalescing, zero-length records, opaque binary content/type/version preservation, the 18,432-byte limit, representable 18,433/65,535 rejection and impossible 65,536 encoding. It covers independent directions, IPv4/IPv6 and supported extension chains, retransmissions, wraparound, gap/overlap/conflict, sticky failures, FIN/RST, incomplete suffixes, factory-only immutable values and consumption ownership. Large/long streams exercise actual TCP buffer reclamation; weak references verify completed records and source objects are released.

`test_tls_record_lifecycle.py` uses the real coordinator/window/session path for LDAP/DNS precedence, port eligibility, client-port/direction interleaving, closure, inactivity/capacity separation, source release and retry after infrastructure/allocation/publication failure. A composed future-consumer failure exercises the existing prepare boundary without adding a semantic parser. Tests compare generic version 1 and all 49 projected values with TLS ingestion disabled. Completed records are observed with a wrapper around the real updater; no replacement capture/session infrastructure or retained record archive is added.

The same synthetic segmented/coalesced IPv4/IPv6 bidirectional traffic passes through little-/big-endian microsecond/nanosecond PCAP sources and real sessions. This is synthetic fixture validation, not external-corpus validation. The replay includes record headers/payloads, stream metadata, a TCP gap and capacity closure under all nine combinations of `PYTHONHASHSEED=1,29,503` and `TZ=UTC,Asia/Kolkata,America/New_York`. Supported IPv6 hop-by-hop/routing/destination-option chains are covered; the generic helper's fixed eight-byte extension encoding is not used to claim Authentication Header support.

Run `PYTHONPATH=src python3 -B -m unittest tests.test_tls_record_framing tests.test_tls_record_lifecycle` before existing TCP, DNS Features 12–22, LDAP, IPv4/IPv6, UDP and system regressions, followed by the complete suite. Compatibility fixtures may exclude the appended `tls_record_state` when constructing historical positional states or replacing unrelated identity-bearing components. The TLS state field is not part of the generic feature snapshot or ML projection.


## TLS handshake-message framing

`test_tls_handshake_framing.py` exercises four-byte handshake headers, all header/body split positions, one-byte record payloads, seeded TLS-record/TCP fragmentation, coalesced messages and partial suffixes. It preserves opaque type/body bytes, ignores non-22 and empty record payloads, checks exact 262,144-byte maximum bodies across real lower-layer buffer reclamation, rejects representable oversized declarations and distinguishes impossible wire values. Tests cover directional IPv4/IPv6 isolation, extension chains, lower-layer failures, retransmissions, wraparound, FIN/RST, no resynchronization, immutable factory-only values, replay consumption and completed-object release. Source dependency checks exclude transport-byte access and semantic parsing.

`test_tls_handshake_lifecycle.py` exercises real coordinator/window/session ownership, protocol precedence, interleaving, closure/inactivity/capacity separation, capture failure, allocation/extraction/publication retry, and packet/window/message weak-reference release. Generic feature version 1 and all 49 projected values are compared after removing only handshake state. Existing constructor/identity fixtures accommodate the appended `tls_handshake_state`; no historical protocol behavior is changed.

Synthetic cross-record handshake traffic passes through the existing little-/big-endian microsecond/nanosecond PCAP readers and real sessions. Wrappers observe the actual updater's transient outputs without replacing capture or framing. Replay checks exact message metadata/body, directional state, ignored records, TCP gap, capacity closure and lifecycle release under all nine `PYTHONHASHSEED=1,29,503` / `TZ=UTC,Asia/Kolkata,America/New_York` combinations. No external-corpus validation or semantic TLS validity is claimed.

Run `PYTHONPATH=src python3 -B -m unittest tests.test_tls_handshake_framing tests.test_tls_handshake_lifecycle` first, then existing TCP/TLS record, DNS, LDAP, IPv4/IPv6, UDP and system regressions, followed by the complete suite.


## TLS handshake-message structural statistics

`test_tls_handshake_statistics.py` verifies empty/default aggregates, zero-length messages, exact extrema/totals/Fraction means, recurring fractions, all 256 type bins, immutable directional separation and constructor/source invariants. It includes a maximum-sized message through existing framing, arbitrary-precision cumulative counters, long bounded reduction, no source graph retention and source dependency checks. Forged inconsistent observations exercise validation without modifying the framer or repairing bytes.

`test_tls_handshake_statistics_lifecycle.py` exercises real coordinator/window/session accounting across TCP and TLS record segmentation, coalesced batches, partial suffixes, retransmissions, lower-layer failures, FIN/RST, closure/inactivity/capacity, publication/aggregation/construction retry, source release and directional wire order. Compatibility fixtures accommodate the appended aggregate field. The generic feature contract and all 49 projected values are compared with statistics reduction disabled.

Synthetic IPv4/IPv6 traffic, bidirectional all-type distributions and cross-record messages pass through little-/big-endian microsecond/nanosecond PCAP sources and real sessions. Deterministic replay includes exact means, fixed bins, TCP failure, capacity closure and release results under all nine `PYTHONHASHSEED=1,29,503` / `TZ=UTC,Asia/Kolkata,America/New_York` combinations. No external corpus or semantic TLS analysis is claimed.

Run `PYTHONPATH=src python3 -B -m unittest tests.test_tls_handshake_statistics tests.test_tls_handshake_statistics_lifecycle` first, then existing handshake/record framing, TCP, DNS, LDAP, IPv4/IPv6, UDP and system regressions, followed by the complete suite.

## TLS ClientHello structural analysis

`test_tls_client_hello.py` covers exact field bytes/order, every required prefix truncation, maximum wire vectors, empty/odd cipher vectors, compression bounds, optional extensions, trailing bytes, selected vector boundaries, nonempty binary ALPN names, unknown/duplicate occurrences, the extension-count ceiling, immutable factory-only values, infrastructure failures and source release. Maximum-size tests distinguish the 131619-byte largest structural body from the 262144-byte handshake ceiling. Tests do not manufacture impossible valid layouts.

`test_tls_client_hello_lifecycle.py` exercises the real packet/coordinator/window/session path for incremental and cross-record messages, seeded fragmentation, both directions, IPv4/IPv6 extension chains, protocol precedence, lower-layer failures, FIN/RST, capacity/inactivity/explicit closure, retry/publication atomicity, bounded current batches and weak-reference release. Existing TLS framing/statistics and all 49 generic projected values are preserved. Compatibility edits only accommodate the appended result tuple in historical constructor/identity fixtures.

Four synthetic classic PCAP encodings (little-/big-endian microsecond/nanosecond) run through the actual capture/session path. A wrapper observes successful manager publication without replacing capture, framing or parsing. Replay includes exact random/session/extension bytes and selected tuples, IPv4/IPv6, both directions, coalesced/cross-record messages, malformed/incomplete inputs, duplicates, unknown extensions, TCP failure, capacity closure and release across all nine required hash-seed/timezone combinations. This is not external-corpus validation or proof of TLS semantic validity.

Run `PYTHONPATH=src python3 -B -m unittest tests.test_tls_client_hello tests.test_tls_client_hello_lifecycle` first, then existing TLS statistics/handshake/record, TCP, DNS, LDAP, IPv4/IPv6, UDP, detection/evaluation/metrics/capture/CLI/system regressions and the complete suite.

## TLS ClientHello structural statistics

`test_tls_client_hello_statistics.py` verifies empty and populated immutable aggregates, exact extrema/totals, fixed distributions, duplicate and unknown extension occurrence semantics, selected-extension cardinalities, directional isolation, non-complete exclusion, lifecycle closure, source release and nine deterministic seed/timezone replays. The reducer is exercised through Feature 26 structural representations and the existing flow/window path.

Run `PYTHONPATH=src python3 -B -m unittest tests.test_tls_client_hello_statistics` first, then the Feature 26 ClientHello tests and the complete suite.

## TLS ServerHello structural analysis

`test_tls_server_hello.py` covers bounded ServerHello fields, optional and empty extension blocks, duplicate and unknown extensions, maximum values, truncation and malformed boundaries, unsupported handshake types, directional IPv4/IPv6 flow paths, ClientHello coexistence, current-batch publication, closure, retry atomicity, source release, four classic PCAP encodings and nine deterministic seed/timezone replays.

Run `PYTHONPATH=src python3 -B -m unittest tests.test_tls_server_hello` first, then the TLS framing/ClientHello/statistics suites and the complete suite.

## TLS ServerHello structural statistics

`test_tls_server_hello_statistics.py` verifies empty and populated immutable aggregates, ordered sparse wire-domain distributions, exact length extrema/totals, extension presence and duplicate semantics, directional isolation, non-complete exclusion, boundary rejection, lifecycle closure, retransmission suppression, publication retry atomicity, source release, four classic PCAP encodings and nine deterministic seed/timezone replays.

Run `PYTHONPATH=src python3 -B -m unittest tests.test_tls_server_hello_statistics` first, then the ServerHello analysis and TLS framing/statistics suites followed by the complete suite.

## IPv6 extension-header structural statistics

`test_ipv6_extension_header_statistics.py` verifies empty and populated immutable aggregates, empty and duplicate chains, directional isolation, IPv4 compatibility, sparse 8-bit distributions, chain and metadata bounds, lifecycle closure, inactivity and capacity boundaries, publication retry atomicity, source release, four classic PCAP encodings and nine deterministic seed/timezone replays.

Run `PYTHONPATH=src python3 -B -m unittest tests.test_ipv6_extension_header_statistics` first, then the IPv6 packet/flow, capture/session and complete suites.

## TCP option structural statistics

`test_tcp_option_statistics.py` exercises real IPv4/IPv6 packet decoding and directional flow observation, EOL/NOP and unknown options, MSS/window-scale extrema and complete wire-domain distributions, SACK block bounds, duplicate kinds, invalid known lengths with atomic prefix exclusion, malformed envelopes, truncated headers, fragments, invalid metadata, immutability and source release. It also checks repeated observations, failed candidate/publication retries, all closure reasons, replacement failures, capture/consumer failure cleanup, DNS/LDAP/TLS coexistence, actual flow-session parity across four classic PCAP encodings, and the established nine seed/timezone replay combinations. Successful repeated packet observations count again; failed publication attempts do not.

Run `PYTHONPATH=src python3 -B -m unittest tests.test_tcp_option_statistics` first, then TCP options/transport, flow lifecycle, affected constructor/field-list regressions, existing replay invariants and the full suite. Compatibility changes to older fixtures only accommodate the appended scalar aggregate; no detector or feature-vector definitions change.

## IP hop-limit statistics

`test_ip_hop_limit_statistics.py` covers IPv4 TTL/IPv6 Hop Limit across TCP/UDP and both directions, all 256 values, repeated observations, exact rational summaries, sparse ordering and cardinality, invalid types/metadata, maximum represented payload lengths, existing fragment/ICMP admission, lower-layer failure exclusion, frozen snapshots and source release. Lifecycle checks cover failed candidates, publication/retry, all closure reasons, replacement/finalization failures, timestamp ordering and capture/consumer cleanup. DNS, LDAP, TLS, TCP options and IPv6 extension statistics coexist with the new aggregate; detector decisions and evaluation metrics remain unchanged.

The real capture/session test compares all coordinated results across little-/big-endian microsecond/nanosecond PCAP encodings. Replay serializes every new stored field and derived summary through flow churn and all closure reasons, then compares SHA-256 across `PYTHONHASHSEED=1,29,503` and `TZ=UTC,Asia/Kolkata,America/New_York`. Run `PYTHONPATH=src python3 -B -m unittest tests.test_ip_hop_limit_statistics`, affected IP/protocol and lifecycle regressions, existing replay invariants, and the complete suite. Older constructor and field-list fixtures only accommodate the appended aggregate.
